# Use an official Python runtime as a parent image
FROM python:3.12-slim

# Set the working directory in the container
WORKDIR /app

# Copy just the requirements file to leverage Docker cache
COPY ./requirements.txt .

# Install any needed packages specified in requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

# Copy the rest of the application code
COPY . .

# Make port available to the world outside this container. Cloud Run uses $PORT.
EXPOSE 8080

# Run uvicorn. The $PORT environment variable is provided by Cloud Run.
# Default to 8080 if $PORT is not set (for local runs).
CMD ["sh", "-c", "uvicorn server.main:app --host 0.0.0.0 --port ${PORT:-8080}"]